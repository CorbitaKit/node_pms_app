enum Roles {
    ADMIN
    DEVELOPER
    MANAGER
    TESTER
}

model Role {
    id        Int      @id @default(autoincrement())
    name      Roles    @unique
    created_at DateTime @default(now())
    updated_at DateTime @updatedAt
    deleted_at DateTime?
    user       User[]
}
